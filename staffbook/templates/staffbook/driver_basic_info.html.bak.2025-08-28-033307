{% extends "staffbook/base_staffbook.html" %}
{% block content %}
{% load driver_extras %}
<div class="container mt-4">
  <div class="card shadow {% if driver.employ_type == '3' %} bg-light text-muted {% endif %}" style="width:90vw;max-width:1400px;margin:0 auto;">
    <div class="card-header bg-white d-flex align-items-center" style="font-size:1.3em;">
      <i class="fa fa-user me-2"></i>
      <b>{{ driver.name }}</b>
      <span class="badge bg-primary ms-2" style="font-size:0.9em;">No.{{ driver.driver_code }}</span>

      {% if driver.employ_type == '3' %}
        <span class="badge bg-danger ms-2" style="font-size:0.85em;">退職者</span>
      {% endif %}

      <span class="ms-auto d-flex align-items-center" style="font-size:0.95em;">
        <span>
          事業者名: {{ driver.company|default:"-" }}　営業所名: {{ driver.workplace|default:"-" }}
        </span>

        {% if missing_items %}
          <span class="badge bg-danger ms-3" style="font-size:0.85em;">
            ⚠ 未提出資料あり
          </span>
        {% endif %}
      </span>
    </div>

    <div class="card-body">
      {% include "staffbook/driver_tab_nav.html" %}

      {% if missing_items %}
        <div class="alert alert-warning mt-3">
          <h5>📌 この従業員は、以下の入社資料が未完了です：</h5>
          <ul class="mb-0">
            {% for label, link in missing_items %}
              <li>❌ <a href="{{ link }}">{{ label }}</a></li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}

      {% if driver.employ_type == '3' %}
        <div class="alert alert-warning mt-3">
          <b>⚠ この従業員は退職済みです。</b><br>
          {% if driver.resigned_date %}
            退職日：<strong>{{ driver.resigned_date|date:"Y/m/d" }}</strong>
          {% else %}
            退職日未入力
          {% endif %}
        </div>
      {% endif %}

     <div class="row">

  <!-- 左列 -->
  <div class="col-md-7">
    <dl class="row mb-2">

      <dt class="col-sm-3">氏名</dt>
      <dd class="col-sm-9">{{ driver.name|default:"-" }}</dd>

      <dt class="col-sm-3">フリガナ</dt>
      <dd class="col-sm-9">{{ driver.kana|default:"-" }}</dd>

      <dt class="col-sm-3">国籍</dt>
      <dd class="col-sm-9">{{ driver.nationality|default:"-" }}</dd>

      <dt class="col-sm-3">事業者名</dt>
      <dd class="col-sm-9">{{ driver.company|default:"-" }}</dd>

      <dt class="col-sm-3">営業所</dt>
      <dd class="col-sm-9">{{ driver.workplace|default:"-" }}</dd>

      <dt class="col-sm-3">部門</dt>
      <dd class="col-sm-9">{{ driver.department|default:"-" }}</dd>

      <dt class="col-sm-3">職種</dt>
      <dd class="col-sm-9">{{ driver.get_position_display|default:"-" }}</dd>

      <dt class="col-sm-3">性別</dt>
      <dd class="col-sm-9">{{ driver.get_gender_display|default:"-" }}</dd>

      <dt class="col-sm-3">電話番号</dt>
      <dd class="col-sm-9">{{ driver.phone_number|default:"-" }}</dd>

      <dt class="col-sm-3">血液型</dt>
      <dd class="col-sm-9">{{ driver.get_blood_type_display|default:driver.blood_type|default:"-" }}</dd>

      <dt class="col-sm-3">生年月日</dt>
      <dd class="col-sm-9">{{ driver.birth_date|date:"Y/m/d"|default:"-" }}</dd>

      <dt class="col-sm-3">年齢</dt>
      <dd class="col-sm-9">
        {% if driver.birth_date %}{{ driver.birth_date|age }}歳{% else %}-{% endif %}
      </dd>

      <dt class="col-sm-3">入社年月日</dt>
      <dd class="col-sm-9">{{ driver.hire_date|date:"Y/m/d"|default:"-" }}</dd>

      <dt class="col-sm-3">選任年月日</dt>
      <dd class="col-sm-9">{{ driver.appointment_date|date:"Y/m/d"|default:"-" }}</dd>

      <dt class="col-sm-3">作成年月日</dt>
      <dd class="col-sm-9">{{ driver.create_date|date:"Y/m/d"|default:"-" }}</dd>

    </dl>
  </div>

  <!-- 右列 -->
  <div class="col-md-5">
    <dl class="row mb-2">
      <dt class="col-sm-3">特記事項</dt>
      <dd class="col-sm-9">{{ driver.remark|default:"-" }}</dd>
    </dl>
  </div>

</div>

<div class="mt-4 text-end">
  <a class="btn btn-primary" href="{% url 'staffbook:driver_basic_edit' driver.id %}">編集</a>
</div>
{% endblock %}